<h1>お気に入り投稿</h1>

<% favorites.each do |favorite| %>
  <div><%= image_tag(favorite.image.url) if favorite.image.url %></div>
  <div><%= favorite.content %></div>

  <div><%= link_to '確認する', post_path(favorite.id) %></div>
<% end %>
